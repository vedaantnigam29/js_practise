(function chai(){
    console.log(`db connected`);
    
})(); // semi colon neccessary
// (this perenthesis for func describtion)(this one is for execution)
(
    (name)=>{
        console.log(`db connected two ${name}`);
        
    }
)("vedaant");